# Terry v1.0.0 — pip install fallback
# Use this if 'pip install -e .' fails due to build system issues:
#   pip install -r requirements.txt
anthropic>=0.105.2
openai>=2.41.0
httpx>=0.28.0
python-dotenv>=1.2.2
rich>=15.0.0
typer>=0.26.7
pyyaml>=6.0.0
tiktoken>=0.7.0
